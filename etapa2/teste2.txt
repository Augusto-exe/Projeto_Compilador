int meu[10], novo, aux;
string abc;
int nome_fun ( const string abc, int guto) {
    static const string pala <= "aaa", gu, new;
    const int a <= -11, b;
    float b<= -7.5;
    bool minha_bool;
    minha_bool = true;   
    continue;
    a >> 2;
    //a >> -2;
    //a << -1;
    a << 0;

    boolvar = a? ( b ? 1 : 2) : (var3 && var4);
    pointer_val = *(a);
    if(true){ aux = 1+2;};

    /* a<=2; 

	*/
}

