/*é obvio que esse codigo não funciona,
  não tenha esperanças 1234 12 13*/

public class xxx
{
        const aa = 6
        //tá tudo errado
        private float _numero = 2.5;

        static int func() {
                int a = 0, numero1[4] = {0, 1, 2, 3};
                bool minhaBool123 = 1;
                char num1num = 'a';
                string minha_string = "int a = 2;";

                do {
                        a = a + 2;
                        a = a - 4;
                        a = a * 2.5;
                        a = _numero / 1;

                        if (a < 0)
                                umafuncqualquer("numero negativo");
                        then (a == 0)
                                umafuncqualquer("%f, %d, %d", numero1, &numero1, *numero1);
                        else
                                umafuncqualquer("vai cair aqui?");
                } while (a > 0);
                foreach (int aaa in numero1)
                {
                        for (int x = 0; x < aa; x++)
                                aaa += x;

                        switch (aaa)
                        {
                                case 0:
                                        umafuncqualquer("é zero");
                                        break;
                                case default:
                                        umafuncqualquer("é nada");
                        }
                }
                

                return 0;
        }
}

end
