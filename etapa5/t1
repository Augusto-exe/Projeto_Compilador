int a,b,c;

int main(int j)
{
	int b,a <= 2;

	if(1)
	{
		b = 1 + 2 + 3 * 4 * (7-2) / 8;
	}
	else
	{
		b = 4+5;
	};

	while(1) do
	{
		a =3;
	};

	char c,d;
}
float h;
int g;
int mn(int j)
{
	int b,a <=2;
	if(1 < 2)
	{
		b = 1 + 2 + 3 * 4 * (7-2) / 8;
	};

	while( 2 > 3) do
	{
		a =3;
	};
	b = 2 ==2 || 1 < 3 && 3 <= 5;
	char c,d;
}

