int a,b,c;

int main(int j)
{
	int b,a <=2;
	b = 1 + 2 + 3 * 4 * (7-2) / 8;
	b = 2 ==2 || 1 < 3 && 3 <= 5;
	char c,d;
}
float h;
int g;
int mn(int j)
{
	int b,a <=2;
	b = 1 + 2 + 3 * 4 * (7-2) / 8;
	b = 2 ==2 || 1 < 3 && 3 <= 5;
	char c,d;
}

